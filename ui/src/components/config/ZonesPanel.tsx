import { t } from '@lingui/core/macro'

export function ZonesPanel() {
  return (
    <div className="flex items-center justify-center flex-1 text-text-muted text-sm">
      {t`Zones editor — coming soon`}
    </div>
  )
}
